query GetRecords {
  getRecords {
    ...RecordInfo
  }
}

query GetConsentRequests {
  getConsentRequests {
    id
    content
    doctor {
      id
      name
    }
    record {
      id
    }
  }
}
