query GetRecords {
  getRecords {
    id
    title
    description
    category
  }
}

query GetRecord($getRecordId: Int!) {
  getRecord(id: $getRecordId) {
    record {
      id
      category
      description
      title
    }
    attachments {
      id
      url
    }
  }
}
