mutation AddPatient($patientEmail: String!) {
  addPatient(patientEmail: $patientEmail)
}

mutation CreateConsentRequest(
  $content: String!
  $recordId: Float!
  $patientId: String!
) {
  createConsentRequest(
    content: $content
    recordId: $recordId
    patientId: $patientId
  )
}
